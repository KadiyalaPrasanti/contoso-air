apiVersion: v1
kind: Service
metadata:
  name: prasanti-test
  namespace: test
  labels:
    app.kubernetes.io/name: prasanti-test
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: prasanti-test
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80